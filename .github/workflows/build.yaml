name: Build

on:
  push:
    branches:
      - main
      - master
    tags:
      - 'v*'
  pull_request:

jobs:
  build:
    runs-on: ubuntu-26.04
    steps:
    - uses: actions/checkout@v3
    - run: cargo build --release
