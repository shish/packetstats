name: Test & Publish

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:

jobs:
  build:
    runs-on: ubuntu-26.04
    steps:
    - uses: actions/checkout@v3
    - run: cargo build --release
